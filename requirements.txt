# Exact versions this project was built with (checked 2026-09-16).
# The GPU (CUDA) build of PyTorch comes from PyTorch's own package index:
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.13.0+cu126
numpy==2.3.2
PyYAML==6.0.3
matplotlib==3.10.5
